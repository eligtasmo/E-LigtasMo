import ResidentUpdates from "./ResidentUpdates";

const ResidentHazardMap = () => {
  // Render the combined Weather/Hazard page; it will default to the Hazard tab based on route
  return <ResidentUpdates />;
};

export default ResidentHazardMap;